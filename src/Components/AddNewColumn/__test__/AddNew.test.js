import {render, screen, fireEvent, within} from "@testing-library/react";
import userEvent from '@testing-library/user-event'

describe("functionalities of add new component", () => {
    it("add new ticket presence", () => {

    })
})